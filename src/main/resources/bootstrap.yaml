spring:
  application:
    name: cake-service
  cloud:
    config:
      uri: http://localhost:9000
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 25
